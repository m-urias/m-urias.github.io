# Book settings
title: My Money and Banking Course
author: Marshall Urias

# Execution settings
execute:
  execute_notebooks: 'auto'

# Master document
root: intro.md

# HTML settings
html:
  extra_navbar: Powered by Jupyter Book
  baseurl: /

# Ensure images are copied
sphinx:
  config:
    mathjax_path: https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js
    html_theme_options:
      navigation_depth: 3
    # Explicitly tell Sphinx to copy the figs directory
    html_static_path: ["figs"]
    html_extra_path: ["figs"]
    # Allow hidden code blocks
    nb_execution_show_tb: True
    # Custom build
  extra_extensions:
    - sphinx.ext.imgconverter
    - sphinx_design